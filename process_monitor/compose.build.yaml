services:
  prod:
    build:
      context: .
      target: prod
      args:
        CADVISOR_MONITOR_DIAGNOSTICS: "${CADVISOR_MONITOR_DIAGNOSTICS}"
    image: cadvisor_prod:latest

  dev:
    build:
      context: .
      target: dev
    image: cadvisor_dev:latest

